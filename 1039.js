// baekjoon 1039
const fs = require('fs')
let input = fs.readFileSync('./dev/stdin').toString().split('\r\n')

const [N, K] = input[0].split(' ').map(e => parseInt(e))
const result = 0
console.log(result)
