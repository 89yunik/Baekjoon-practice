const fs = require('fs')
const input = fs.readFileSync('./dev/stdin').toString().split('\r\n')
// const input = fs.readFileSync('/dev/stdin').toString().split('\n')

//input[0].split(' ').map(e => parseInt(e))
const answer = 'Hello World!'
console.log(answer)
